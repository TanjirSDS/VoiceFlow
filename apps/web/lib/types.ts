// StoredAgentConfig moved to '@voiceflow/engine/templates' (Phase 10) so the
// migrate script can share it. Re-exported here for existing import sites.
export type { AgentType, StoredAgentConfig } from '@voiceflow/engine/templates'
export { normalizeStoredConfig, normalizeStoredConfigSafe } from '@voiceflow/engine/templates'
